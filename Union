#include <stdio.h>

union Data {
    int age;
    float marks;
};

int main() {
    union Data d;
    d.age = 18;
    printf("age = %d\n", d.age);
    d.marks = 89.5;
    printf("Marks = %f\n", d.marks);
    printf("age = %f\n",d.age);
}
